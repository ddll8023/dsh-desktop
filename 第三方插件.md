# DeepSeek Harness 第三方插件推荐

本文档用于帮助其他人在自己的 DeepSeek Harness（DSH）环境中下载并启用当前使用的第三方插件和 Agent Preset。

## 一、插件 / Agent Preset 清单

| 插件 / Agent Preset             | 作用                                                                                                                                              | 下载地址                                                                                             |
| ------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| dsh-codex                       | 接入 OpenAI Codex（ChatGPT Plus/Pro OAuth）模型 Provider；提供`/codex login`、模型选择、SSE/工具调用流、原生 Web Search，以及 Web UI 与命令行用量/配额显示。 | `https://github.com/ddll8023/dsh-codex.git`                                                        |
| dsh-access-mode                 | 提供 Default / No Edit / Auto 三种会话访问模式，替换原生 Permission Presets 选择器。                                                              | `https://github.com/ddll8023/dsh-access-mode.git`                                                  |
| @deepseek-ai/dsh-plugin-console | DSH Web 设置页插件管理面板：浏览 GitHub dsh-plugin 插件市场，一键安装、启用、停用插件。                                                           | `https://github.com/Noob-stupid/dsh-plugin-hub.git`                                                |
| @linxin666/dsh-web-ui-all       | DSH Web UI 全家桶聚合插件，包含 task-board、git-graph、pet、remote-web-ui、live-stats、web-ui-settings、skins、ssh、aionui-panel 等 UI/功能插件。 | npm：`@linxin666/dsh-web-ui-all@0.1.12`GitHub：`https://github.com/zhu1090093659/dsh-web-ui.git` |
| @huanlin/dsh-plugin-session-delete | 在 Web 界面删除会话：会话头部垃圾桶、侧栏“删除会话”菜单，删除会话日志/投影缓存/工作区记账，并提供 `workbench_session_delete` agent 工具。 | `https://github.com/lsz-asd/dsh-plugin-session-delete.git` |
| dsh-memory-evolve                | 跨会话长期记忆（五轨）、待办、技能管理、COI 调度、会话广播、提示词、会话搜索等；默认多数功能关闭，可在 Memory Evolve 设置中开启。               | `https://github.com/csyangwen/dsh-memory-evolve.git`                                              |
| dsh-rewind                       | 会话回退/恢复/分支继续：OpenChamber 风格时间线、消息行 ↩、项目文件回滚；基于 Da-Mie/dsh-rewind 修改，新增回退方式选择弹窗。                     | `https://github.com/ddll8023/dsh-rewind.git`                                                         |
| dsh-at-file                      | 输入框 `@` 路径引用（Codex 风格）：在 composer 键入 `@` 搜索当前工作区文件/目录并插入路径引用，提交时生成 `<workspace-reference>` 标记供 agent 读取；含路径选择器、目录进入、文件名过滤规则设置页。 | `https://github.com/omdsh-dev/dsh-at-file.git`（v0.6.0）                              |
| dsh-anchored-standard           | DSH Agent Preset（非插件）：优化 DeepSeek，让其符合官方分数。                                                                                     | `https://github.com/xiaobright/dsh-anchored-standard.git`                                          |

> `dsh-codex` 和 `dsh-access-mode` 已发布到 GitHub：[https://github.com/ddll8023/dsh-codex](https://github.com/ddll8023/dsh-codex) 和 [https://github.com/ddll8023/dsh-access-mode](https://github.com/ddll8023/dsh-access-mode)。
>
> `dsh-codex` 的用量窗口名称和时长以 ChatGPT 用量接口返回的 `limit_window_seconds` 为准；`primary_window` 不应固定理解为 5 小时。缺少服务端时长时，插件会显示“主要窗口”，避免误导。
>
> `dsh-anchored-standard` 是 Agent Preset，不是 `dsh plugin` 插件，不能通过 `dsh plugin add` 安装；安装方法见「方法四」。

## 二、前置要求

- DeepSeek Harness（dsh）`>= 0.1.0-rc.6`
- Node.js 和 pnpm（`dsh plugin` 会转发给 pnpm，可用 `corepack enable pnpm` 启用）
- Git（用于 clone 插件/预设源码）
- 能访问 npm registry 和 GitHub

## 三、下载方法

### 方法一：使用 `dsh plugin` 命令（推荐）

已发布到 npm 或可直接通过 GitHub 安装的插件，用一条命令安装并自动加入 `web` profile 的 bundle 层：

```bash
# dsh-codex
dsh plugin --profile web add github:ddll8023/dsh-codex

# dsh-access-mode
dsh plugin --profile web add github:ddll8023/dsh-access-mode

# dsh-plugin-console
dsh plugin --profile web add github:Noob-stupid/dsh-plugin-hub

# dsh-web-ui-all
dsh plugin --profile web add @linxin666/dsh-web-ui-all@0.1.12

# dsh-plugin-session-delete
dsh plugin --profile web add github:lsz-asd/dsh-plugin-session-delete

# dsh-memory-evolve
dsh plugin --profile web add github:csyangwen/dsh-memory-evolve

# dsh-rewind
dsh plugin --profile web add github:ddll8023/dsh-rewind

# dsh-at-file（输入框 @ 文件引用，v0.6.0）
dsh plugin --profile web add https://github.com/omdsh-dev/dsh-at-file/archive/refs/tags/v0.6.0.tar.gz
```

如果不想让 `dsh plugin` 直接拉取 GitHub 仓库，也可以先 clone 到本地，再安装本地路径：

```bash
git clone https://github.com/ddll8023/dsh-codex.git
git clone https://github.com/ddll8023/dsh-access-mode.git

dsh plugin --profile web add /绝对路径/dsh-codex
dsh plugin --profile web add /绝对路径/dsh-access-mode
```

安装后重启 `dsh web` 使插件生效。

### dsh-codex 用量显示

登录并选择 OpenAI Codex 模型后：

- `/codex status`：显示登录状态、账号信息和当前配额。
- `/codex usage`：只查询当前账号配额。
- Web GUI 输入框下方：显示用量摘要，点击后查看各配额窗口的已用比例、剩余比例、倒计时和绝对重置时间。

窗口名称和时长来自 ChatGPT 用量接口的 `limit_window_seconds`。因此 `primary_window` 可能是 5 小时、1 小时或其他时长，不能默认写成 5 小时；如果服务端没有返回时长，界面会显示“主要窗口”。

如果出现 `codexUsage/get` 404，优先检查插件与 DSH Runtime 是否解析到同一份 `@deepseek-ai/dsh-typert-protocol`，然后重启 `dsh web`。

> `dsh-memory-evolve` 不声明 `dsh.bundle`，`dsh plugin add` 只会把它安装为普通依赖，不会自动加入 bundle 层。安装后还需在 `~/.dsh/profiles/web/cordis.patch.yml` 末尾追加以下内容注册插件：
>
> ```yaml
> - insert:
>     - id: dsh-memory-evolve
>       name: dsh-memory-evolve
> ```
>
> 然后重启 `dsh web`。

> `dsh-rewind` 在 macOS 上按 `Alt+T` 无反应的已知问题：插件原判断使用 `event.key === "t"`，而 macOS Option+T 的 `event.key` 是 `™`。本地安装时可将 `lib/client.js` 中快捷键判断改为同时匹配 `event.code === "KeyT"`，改后刷新页面即可。

### 方法二：一键下载脚本

脚本会把以下六个会自动注册到 bundle 层的插件源码 clone 到 `$HOME/dsh-plugins`（可通过环境变量/参数修改），然后逐个执行 `dsh plugin --profile <profile> add <本地路径>`。`dsh-memory-evolve` 因需要额外手动 patch，不包含在该脚本中，请按「方法一」安装。

#### macOS / Linux（bash）

将以下内容保存为 `download-plugins.sh`，脚本默认使用已发布地址，也可通过环境变量覆盖：

```bash
#!/usr/bin/env bash
set -euo pipefail

# 可覆盖项
DSH_PROFILE="${DSH_PROFILE:-web}"
PLUGIN_DIR="${DSH_PLUGIN_DIR:-$HOME/dsh-plugins}"
DSH_CODEX_REPO_URL="${DSH_CODEX_REPO_URL:-https://github.com/ddll8023/dsh-codex.git}"
DSH_ACCESS_MODE_REPO_URL="${DSH_ACCESS_MODE_REPO_URL:-https://github.com/ddll8023/dsh-access-mode.git}"
DSH_PLUGIN_CONSOLE_REPO_URL="${DSH_PLUGIN_CONSOLE_REPO_URL:-https://github.com/Noob-stupid/dsh-plugin-hub.git}"
DSH_WEB_UI_REPO_URL="${DSH_WEB_UI_REPO_URL:-https://github.com/zhu1090093659/dsh-web-ui.git}"
DSH_SESSION_DELETE_REPO_URL="${DSH_SESSION_DELETE_REPO_URL:-https://github.com/lsz-asd/dsh-plugin-session-delete.git}"
DSH_REWIND_REPO_URL="${DSH_REWIND_REPO_URL:-https://github.com/ddll8023/dsh-rewind.git}"

for cmd in git dsh pnpm; do
  if ! command -v "$cmd" >/dev/null 2>&1; then
    echo "错误：缺少命令 $cmd，请先安装。pnpm 可通过 corepack enable pnpm 启用。" >&2
    exit 1
  fi
done

mkdir -p "$PLUGIN_DIR"
PLUGIN_DIR="$(cd "$PLUGIN_DIR" && pwd)"

clone_or_pull() {
  local repo="$1" dir="$2"
  if [[ -d "$dir/.git" ]]; then
    git -C "$dir" pull --ff-only
  else
    git clone "$repo" "$dir"
  fi
}

echo "==> 下载 dsh-codex"
clone_or_pull "$DSH_CODEX_REPO_URL" "$PLUGIN_DIR/dsh-codex"
dsh plugin --profile "$DSH_PROFILE" add "$PLUGIN_DIR/dsh-codex"

echo "==> 下载 dsh-access-mode"
clone_or_pull "$DSH_ACCESS_MODE_REPO_URL" "$PLUGIN_DIR/dsh-access-mode"
dsh plugin --profile "$DSH_PROFILE" add "$PLUGIN_DIR/dsh-access-mode"

echo "==> 下载 dsh-plugin-console"
clone_or_pull "$DSH_PLUGIN_CONSOLE_REPO_URL" "$PLUGIN_DIR/dsh-plugin-console"
dsh plugin --profile "$DSH_PROFILE" add "$PLUGIN_DIR/dsh-plugin-console"

echo "==> 下载 dsh-web-ui-all"
clone_or_pull "$DSH_WEB_UI_REPO_URL" "$PLUGIN_DIR/dsh-web-ui-all"
dsh plugin --profile "$DSH_PROFILE" add "$PLUGIN_DIR/dsh-web-ui-all"

echo "==> 下载 dsh-plugin-session-delete"
clone_or_pull "$DSH_SESSION_DELETE_REPO_URL" "$PLUGIN_DIR/dsh-plugin-session-delete"
dsh plugin --profile "$DSH_PROFILE" add "$PLUGIN_DIR/dsh-plugin-session-delete"

echo "==> 下载 dsh-rewind"
clone_or_pull "$DSH_REWIND_REPO_URL" "$PLUGIN_DIR/dsh-rewind"
dsh plugin --profile "$DSH_PROFILE" add "$PLUGIN_DIR/dsh-rewind"

echo "完成。请重启 dsh web，再在浏览器中刷新页面。"
```

也可以不修改脚本，通过环境变量传入真实地址：

```bash
DSH_CODEX_REPO_URL=https://github.com/ddll8023/dsh-codex.git \
DSH_ACCESS_MODE_REPO_URL=https://github.com/ddll8023/dsh-access-mode.git \
DSH_REWIND_REPO_URL=https://github.com/ddll8023/dsh-rewind.git \
bash download-plugins.sh
```

#### Windows（PowerShell）

将以下内容保存为 `download-plugins.ps1`，脚本默认使用已发布地址，也可通过参数覆盖：

```powershell
param(
    [string]$ProfileName = "web",
    [string]$PluginDir = (Join-Path $HOME "dsh-plugins"),
    [string]$DshCodexRepoUrl = "https://github.com/ddll8023/dsh-codex.git",
    [string]$DshAccessModeRepoUrl = "https://github.com/ddll8023/dsh-access-mode.git",
    [string]$DshPluginConsoleRepoUrl = "https://github.com/Noob-stupid/dsh-plugin-hub.git",
    [string]$DshWebUiRepoUrl = "https://github.com/zhu1090093659/dsh-web-ui.git",
    [string]$DshSessionDeleteRepoUrl = "https://github.com/lsz-asd/dsh-plugin-session-delete.git",
    [string]$DshRewindRepoUrl = "https://github.com/ddll8023/dsh-rewind.git"
)

$ErrorActionPreference = "Stop"

foreach ($cmd in @("git", "dsh", "pnpm")) {
    if (-not (Get-Command $cmd -ErrorAction SilentlyContinue)) {
        throw "缺少命令 $cmd，请先安装。pnpm 可通过 corepack enable pnpm 启用。"
    }
}

New-Item -ItemType Directory -Force -Path $PluginDir | Out-Null
$PluginDir = (Resolve-Path $PluginDir).Path

function Clone-Or-Pull {
    param([string]$Repo, [string]$Dir)
    if (Test-Path (Join-Path $Dir ".git")) {
        git -C $Dir pull --ff-only
    } else {
        git clone $Repo $Dir
    }
}

Write-Host "==> 下载 dsh-codex"
Clone-Or-Pull $DshCodexRepoUrl (Join-Path $PluginDir "dsh-codex")
dsh plugin --profile $ProfileName add (Join-Path $PluginDir "dsh-codex")

Write-Host "==> 下载 dsh-access-mode"
Clone-Or-Pull $DshAccessModeRepoUrl (Join-Path $PluginDir "dsh-access-mode")
dsh plugin --profile $ProfileName add (Join-Path $PluginDir "dsh-access-mode")

Write-Host "==> 下载 dsh-plugin-console"
Clone-Or-Pull $DshPluginConsoleRepoUrl (Join-Path $PluginDir "dsh-plugin-console")
dsh plugin --profile $ProfileName add (Join-Path $PluginDir "dsh-plugin-console")

Write-Host "==> 下载 dsh-web-ui-all"
Clone-Or-Pull $DshWebUiRepoUrl (Join-Path $PluginDir "dsh-web-ui-all")
dsh plugin --profile $ProfileName add (Join-Path $PluginDir "dsh-web-ui-all")

Write-Host "==> 下载 dsh-plugin-session-delete"
Clone-Or-Pull $DshSessionDeleteRepoUrl (Join-Path $PluginDir "dsh-plugin-session-delete")
dsh plugin --profile $ProfileName add (Join-Path $PluginDir "dsh-plugin-session-delete")

Write-Host "==> 下载 dsh-rewind"
Clone-Or-Pull $DshRewindRepoUrl (Join-Path $PluginDir "dsh-rewind")
dsh plugin --profile $ProfileName add (Join-Path $PluginDir "dsh-rewind")

Write-Host "完成。请重启 dsh web，再在浏览器中刷新页面。"
```

也可以不修改脚本，通过参数传入真实地址：

```powershell
.\download-plugins.ps1 -DshCodexRepoUrl "https://github.com/ddll8023/dsh-codex.git" -DshAccessModeRepoUrl "https://github.com/ddll8023/dsh-access-mode.git"
```

### 方法三：手动安装

1. 将插件源码放入或链接到 `$DSH_HOME/profiles/web/node_modules/<包名>`。
2. 在 `$DSH_HOME/profiles/web/package.json` 的 `dsh.profile.bundles` 中追加对应包名。
3. 重启 `dsh web`。

### 方法四：安装 Agent Preset（dsh-anchored-standard）

该 preset 不是 npm/GitHub 插件，而是需要复制到 DSH 的 agent preset 目录。当前使用的版本来自 [https://github.com/xiaobright/dsh-anchored-standard](https://github.com/xiaobright/dsh-anchored-standard)。

#### macOS / Linux

```bash
git clone https://github.com/xiaobright/dsh-anchored-standard.git
target="$HOME/.dsh/.agent-presets/anchored-standard"
mkdir -p "$HOME/.dsh/.agent-presets"
test ! -e "$target"
cp -R dsh-anchored-standard/preset "$target"
```

#### Windows（PowerShell）

```powershell
git clone https://github.com/xiaobright/dsh-anchored-standard.git
$target = Join-Path $HOME ".dsh\.agent-presets\anchored-standard"
New-Item -ItemType Directory -Force -Path (Split-Path -Parent $target) | Out-Null
if (Test-Path -LiteralPath $target) { throw "Preset already exists: $target" }
Copy-Item -Recurse -LiteralPath ".\dsh-anchored-standard\preset" -Destination $target
```

安装后重启 DSH 并新建会话，在模式中选择 **Anchored Standard (experimental)**；也可以在 `~/.dsh/settings.yaml` 中设为默认：

```yaml
agent-presets:
  default: anchored-standard
```

从旧版本升级时，直接覆盖 `$HOME/.dsh/.agent-presets/anchored-standard/` 下的文件并重启即可。

## 四、安装后验证

```bash
dsh --profile web --dump-config | grep -E "dsh-codex|dsh-access-mode|dsh-plugin-console|dsh-web-ui-all|session-delete|dsh-memory-evolve|dsh-rewind|dsh-at-file"
```

或分别检查：

```bash
dsh --profile web --dump-config | grep -A2 llm-codex             # dsh-codex
dsh --profile web --dump-config | grep -A2 access-mode           # dsh-access-mode
dsh --profile web --dump-config | grep -A2 chameleon-session-delete  # dsh-plugin-session-delete
dsh --profile web --dump-config | grep -A2 dsh-memory-evolve         # dsh-memory-evolve
dsh --profile web --dump-config | grep -A2 dsh-rewind                # dsh-rewind
dsh --profile web --dump-config | grep -A2 dsh-at-file                # dsh-at-file
```

Agent Preset 检查：

```bash
ls -1 "$HOME/.dsh/.agent-presets/anchored-standard"        # 应包含 agent.cordis.yml、preset.yml、tool-bootstrap.mjs
grep -A1 "agent-presets:" "$HOME/.dsh/settings.yaml"       # 应显示 default: anchored-standard
```

重启并刷新 Web GUI 后，应能看到插件对应功能（模型选择器中的 OpenAI Codex、Codex 用量摘要与详情、composer 左侧访问模式、设置页插件管理、Web UI 全家桶组件、会话头部垃圾桶和侧栏“删除会话”菜单、会话页的 Memory Evolve 设置/记忆等 Tab、消息行 ↩ 与 `Alt+T` 会话时间线、输入框键入 `@` 弹出工作区文件/目录选择器）；新建会话时也应能选择 **Anchored Standard (experimental)**。
